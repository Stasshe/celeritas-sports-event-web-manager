services:
  - type: web
    name: sports-event-web-manager
    env: static
    buildCommand: pnpm build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
